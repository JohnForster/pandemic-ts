import styled from 'styled-components';

export const Container = styled.div`
  position: absolute;
  z-index: 400;
  bottom: 0;
  margin: 1rem;
`;

export const Side = styled.div`
  padding: 0.5rem;
  width: 100%;
  height: 100%;
`;
